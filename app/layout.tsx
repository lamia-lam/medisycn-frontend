import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";

import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "MediSync — Unified Healthcare Platform",
  description:
    "MediSync connects doctors, patients, pharmacies, and diagnostic centers on one seamless platform. Manage appointments, prescriptions, and diagnostics with ease.",
  keywords: [
    "healthcare",
    "medical platform",
    "doctor",
    "patient",
    "pharmacy",
    "diagnostics",
    "MediSync",
  ],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
